# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local Netlify folder
.netlify

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Migration export — regenerate with scripts/export-instant.mjs.
# Never committed: contains guest contact details.
migration/instant-export/

# Vitest coverage output.
coverage/

# PowerSync's SQLite worker and WASM, copied out of node_modules by
# scripts/copy-powersync-assets.mjs (run by `pnpm run dev` and `pnpm run
# build`). Generated, version-locked to the SDK, and 2.5 MB.
public/@powersync/
