# Audits

`/audits`. Field work first, then the list.

- The Audits section in full (see `AuditsSection.spec.md`).
- *Launch audit* for `manage_audits`.
- Tabs Open / Closed / Finalized with counts; each audit a card linking to its
  detail with kind, launch date, audited-of-total and, when closed, the
  number of undecided proposals.
